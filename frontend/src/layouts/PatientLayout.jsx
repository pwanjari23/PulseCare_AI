import React from 'react';
import DashboardLayout from './DashboardLayout';

const PatientLayout = () => {
  // Can be customized with patient-specific sidebars or contexts in the future
  return <DashboardLayout />;
};

export default PatientLayout;
