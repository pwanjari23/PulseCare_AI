import React from 'react';
import DashboardLayout from './DashboardLayout';

const DoctorLayout = () => {
  // Can be customized with doctor-specific sidebars or contexts in the future
  return <DashboardLayout />;
};

export default DoctorLayout;
