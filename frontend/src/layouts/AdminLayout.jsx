import React from 'react';
import DashboardLayout from './DashboardLayout';

const AdminLayout = () => {
  // Can be customized with admin-specific sidebars or contexts in the future
  return <DashboardLayout />;
};

export default AdminLayout;
